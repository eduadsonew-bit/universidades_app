name: universidades_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # --- IMPORTANTE: Asegúrate de que estas líneas estén alineadas con 'flutter:' ---
  
  # Para peticiones HTTP
  http: ^1.1.0
  
  # Para manejo de estado
  provider: ^6.0.5
  
  # Para cámara y galería
  image_picker: ^1.0.4

  # El resto de dependencias por defecto
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

